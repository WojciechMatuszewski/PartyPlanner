extend type Message {
  isSendByMe: Boolean!
  optimisticallyAdded: Boolean!
  optimisticallyCreated: Boolean!
  hasOptimisticError: Boolean!
}

enum UserStatus {
  OFFLINE
  ONLINE
}

extend type User {
  status: UserStatus!
}
